@{
    ViewData["Title"] = "Home Page";
}
@using System.Globalization
@using asp_dot_net_core_web_app_mvc_fast_food_system.Models.Base;
@using asp_dot_net_core_web_app_mvc_fast_food_system.Enums;
@using asp_dot_net_core_web_app_mvc_fast_food_system.Models.VM
@model Dictionary<ProductCategory, List<ProductVM>>

<div class="container home-page">
    <div class="products">
        @foreach (KeyValuePair<ProductCategory, List<ProductVM>> productCategory in Model)
        {
            <div class="product-category">
                @* <h2>@productCategory.Key</h2> *@
                <h2>@Html.DisplayFor(model => productCategory.Key)</h2> @* Uses the '[Display(Name = "...")]' data annotation *@

                @foreach (ProductVM product in productCategory.Value)
                {
                    <div class="product">
                        @if (product.Product.Category == ProductCategory.Extras)
                        {
                            <span class="product-code"></span>
                        }
                        else
                        {
                            <span class="product-code">@product.Product.Code</span>
                        }
                        <span class="product-name">@product.Product.Name</span>
                        <span class="product-price">@product.Product.Price.ToString("C", new CultureInfo("en-CA"))</span>
                        <span class="product-quantity @(product.Quantity == 0 ? "hide" : "")">@(product.Quantity)</span>
                    </div>
                }
            </div>
        }
    </div>
</div>
